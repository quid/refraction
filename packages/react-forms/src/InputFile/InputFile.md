The `InputFile` component shares the same API of the HTML `input[type=file]` element.

```js
<InputFile multiple />
```

It also supports all the `InputText` and `Button` properties:

```js
<InputFile size="small" importance="primary" />
```
